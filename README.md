# IAE Event Flow

Event flow application with:

- Frontend: Vite + React (`/`)
- Backend: NestJS (`/backend`)

## Frontend

```bash
npm install
npm run dev
```

## Backend

```bash
cd backend
npm install
cp .env.example .env
npm run start:dev
```

See `backend/README.md` for MSSQL setup and connection checks.
